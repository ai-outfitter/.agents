# Which agents this catalog deploys, and the deployment prefix they render
# under. `organization` renders `__ORG__`; each cluster deploys exactly the
# agents listed for it.
organization: outfitter
clusters:
  nonprod:
    agents:
      - luce
      - vega
      - outfitter-bot
